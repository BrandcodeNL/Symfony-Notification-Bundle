{% macro notification(UserNotification, maxRead, maxUnread) %}

<a href="{% if UserNotification.notification.link is not null%}{{ UserNotification.notification.link }}{% else %}#{% endif %}">
    <div class="notification">
        <h5>{{UserNotification.notification.title}}</h5>        
    </div>
</a>
{% endmacro %}